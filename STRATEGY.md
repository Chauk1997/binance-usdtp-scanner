# Scanner V54：2026-09-26 確認版

策略識別：`V5.9_CONFIRMED_20260926_TRADE_CVD`。正式執行入口為 `scanner_latest.build`；`strategy_latest` 負責純技術判定。舊 V5.8/V5.6 函式只保留供歷史回歸測試，已解除舊策略、交集榜、入場與衍生品手動更新的 HTTP 路由，正式排程不再呼叫。

## 使用者確定的規則

- Binance 可交易 USDT 本位永續全母池，不取漲幅 Top100、不抽樣、不提前湊滿停止；排除股票及 USDC，移除 FDUSD/TUSD/USDP/DAI/BTCDOM 等舊額外黑名單，也不排除非 ASCII 幣名。
- 每輪固定 cutoff，只接受 `close_time < cutoff`。EMA15、SMA30、SMA45；均線均由已收盤資料重算。拒絕重複、斷棒、非有限值、異常 OHLC 及過時歷史。
- Key K **僅以量能決定**：`volume >= previous * 2.2` 且 `volume > mean(previous 24)`，均量排除當根。2.2 邊界以 Decimal 比較，避免浮點誤差。前量為零時倍率顯示 null，但依不等式判定。
- 大實體上漲、短上影、價格推進、開收盤在三線上方及拉動均線屬於品質。上影超過實體一半扣分，不硬排除。下影不扣分：回傳真實 body/range，但排名的實體完整度使用 body/(body+upper)，避免下影暗中降低分數。
- 1H 以 4H 為主要背景，4H 完整空頭發散硬排除；日線只作附加品質，不能否決 1H。4H 以 1D 為背景，日線完整空頭發散硬排除。
- 保留 1H 第一／二／三型、4H 第一／二型；可同時符合多型。型態名稱不進排名鍵，4H 第一型沒有優先名額。
- 特殊型獨立掃全母池：1D 多頭已存在 → 4H 第一型 Key K → 後續已收 4H 回補／整理 → 其後 1H 壓縮 → 新 1H 第一型 Key K。各階段輸出時間，日線使用當時已知資料，禁止事後套入未來日線。單純雙週期爆量不合格。
- 正式特殊型與接近標的始終輸出。接近者須已確認日線背景與 4H 第一型，列完成階段、缺少條件、下一訊號。特殊型不依賴兩張 Top10；不截斷接近清單。
- 全部技術候選確定後才查輔助資料。弱資金資料不淘汰技術候選，亦不救不合格標的；未正式通過的接近者不另外查輔助資料。
- 各自排序取 1H / 4H 最多 10 筆，不足不補。取消 resonance/intersection。沒有止跌、白箭頭或實際入場篩選。

## 需要明示的數值化解讀

原對話與附件只有型態例子，沒有舊第一／二／三型的完整演算法或圖形樣本；既有程式也沒有這三型的對應分類。以下是依「壓縮啟動、結構突破、多頭再加速」例子做的可審查工程定義，**不是聲稱逐像素復刻歷史人工判圖**。參數集中在 `strategy_latest.Config`，每份 feed 帶出全部參數。

| 項目 | 本次明確採用的定義 |
|---|---|
| 近期 Key K | 最近 12 根，延用既有搜尋窗；新鮮度仍進排序 |
| 多頭 | EMA15 > SMA30 > SMA45；三線近 5 根向上及近 3 根擴張加分 |
| 完整空頭發散 | 連續 3 根 SMA45 > SMA30 > EMA15，三線逐根下降，兩個相鄰均線間距（按收盤價正規化）均擴大 |
| 壓縮／盤整 | 前 4 根價格高低幅度 ≤ 6%；且三線寬度全程 ≤ 2%，或末端寬度縮至起點 ≤ 85% |
| 第一型 | 前述壓縮後的 Key K，收盤在三線上方，價格推進、EMA15 與至少一條慢線向上 |
| 第二型 | Key K 收盤突破前 24 根最高價，同樣要求價格推進與均線拉抬；不額外強制與第一型相同的緊密壓縮 |
| 第三型（僅 1H） | Key K 前後皆多頭排列，漲幅大於前根正漲幅，EMA15–SMA45 間距擴大，符合價格推進及均線拉抬 |
| 結構失效 | 當前收盤跌到 SMA45 下方超過 3%（沿用舊結構保存容忍值）；操作級別完整空頭發散亦不列多頭候選 |
| 特殊型歷史窗 | 4H 第一型回看 48 根；新 1H Key K 回看 12 根。歷史不夠時不猜測 |
| 4H 回補 | 後續已收 K 區間接觸任一均線 ±1%，收盤仍在結構保存範圍；或至少四根後符合盤整。其後若收盤跌破結構失效線，整條序列作廢 |
| 1H 特殊壓縮 | 壓縮窗開始在 4H 回補確認之後，該時點最近已收 4H 仍接觸均線或盤整；之後才接受 1H 第一型 |
| 相對強弱 | 同一操作級別最近六根收盤報酬減 BTC 同期報酬；無可靠 BTC 時為 null，無額外補值優勢 |
| 廣泛退潮 | 每榜至少 5 個且占整榜 ≥60% 的標的，同時有可靠同窗口 完整成交資料計算的 CVD<0 和 Taker<1。缺 CVD 記為未知，不當零。各榜分別警示；全域警示須所有非空榜皆廣泛退潮，避免 4H 弱、1H 強被說成全面退潮 |

排名採字典序：主要高週期背景（1H 的日線只微量附加）→ 當前均線結構 → 壓縮品質 → Key K 品質 → 三線拉抬 → 新鮮度 → 相對 BTC 強弱 → 輔助分。同分以 symbol 穩定排序。沒有型態名稱加分，也沒有舊止跌、抗跌、延續或阻力距離特別優先權。

## 輔助來源與限制

使用 Binance `openInterestHist`、`topLongShortPositionRatio`、`topLongShortAccountRatio`、`globalLongShortAccountRatio`、`takerlongshortRatio`、`fundingRate`。可用 `BINANCE_API_KEY` 提供伺服器環境金鑰，程式不輸出其內容。依官方文件，大戶比率端點可能需要 API Key；缺少權限、限流、延迟回傳都保留技術榜並標記缺資料。

- OI 以 base contract quantity 計算同操作級別邊界的變化，不以名目價值漲幅冒充持倉增長。
- OI / 多空比使用文件的結束 timestamp，Taker 使用開始 timestamp，只接受所需完整已結束窗口；不拿錯時段湊值。
- Funding 使用最後已結算費率，另列其時間，不冒充未來預測費率。
- 依使用者追加要求，CVD 改以 Binance `/fapi/v1/aggTrades` 成交資料直接計算：`m=false` 加上 q，`m=true` 減去 q。1H／4H 使用最近完整已收區間，起點歸零；單位為基礎資產成交量。標記 `calculated_from_trades`，不冒充交易所直接發布的 CVD 指標。完整資料才加入排名與退潮警示。
- 大戶與整體比率保留在輸出供判讀；不把高比率武斷當作一定利多。輔助分使用 OI、Taker、可靠 CVD 方向與 funding 擁擠（>0.1%）提醒。
- 每輪重新抓 exchangeInfo，不能退回舊母池假稱完整。股票依資產分類與 `SCANNER_STOCK_SYMBOLS` 明確排除清單處理；未知分類停輪並報出名稱，避免默默漏掃或收進股票。RWA 不等於股票，不一律排除。
- 技術資料任一母池成員缺 K／過時時不發布新的完整快照；不足 50 根均線歷史者列入覆蓋，但診斷為不足歷史、不能正式合格。
- 輔助查詢最多 3 個工作並行、單工作每次請求間隔 0.35 秒；沿用 418/429、Retry-After 退避。候選很多或冷啟動可能需數分鐘，不保證整點 :02 前完成。

官方參考：[USDⓈ-M Market Data](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data)。

## 輸出與遷移

`/scan/feed`、`/scan/feed/summary`、MCP `get_scan_feed` 都提供 1H / 4H、`special.formal`、`special.approaching`、`market_state`。前三個 run 別名仍在，但皆指向相同新策略。舊 `/scan/resonance`、各版本 entry-now / watchlist 等回 404。

舊策略快照在重啟時不載入；新策略未完成前顯示 not_ready。跨收盤時重新核對 1H/4H/1D 依賴，過時榜、特殊型及警示不發布。快照保持原子寫入、失敗保留前一個同版本結果、讀取不觸發掃盤。

執行測試：Python 3.12 環境安裝 `requirements.txt`、`requirements-mcp.txt` 與 pytest，然後 `python -m pytest -q`。既有 numpy 2.0.2 不支援本機 Python 3.13 建置；請用 Python 3.12。未改動原鎖版套件。

## 成交資料 CVD 更新

依使用者追加要求，允許從成交資料直接計算 CVD。使用 q（包含 RPI），只涵蓋 aggTrades 公開市場成交；不涵蓋 ADL／保險基金交易。它是該 1H／4H 窗口內累積淨成交量，不是從任意歷史起點延伸的永久累積曲線。

每次查詢最多 1000 筆，先拆為最多一小時，再對滿額區間二分，直到每段未截斷。檢查 ID 連續、重複 ID、時間、數量與 maker 旗標。每個標的／週期最多 128 次請求，超限、缺資料或限流均回 null，保留技術合格資格。單毫秒滿 1000 筆亦標記不可驗證，不猜測。空區間完整返回才可記零。

Binance 此端點權重為 20，歷史查詢限最近 48 小時；成交量大的標的可能無法在預算內補齊，且完整掃盤耗時可能增加。遵守原有全域節流及 Retry-After。
